name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            build_cmd: pnpm build:linux
          - os: windows-latest
            build_cmd: pnpm build:win
          - os: macos-latest
            build_cmd: pnpm build:mac

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        with:
          version: latest

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install

      # On tags: build + publish to GitHub Release (electron-builder handles it)
      - name: Build & Publish
        if: startsWith(github.ref, 'refs/tags/v')
        run: ${{ matrix.build_cmd }} -- --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # On branches: just build artifacts
      - name: Build
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: ${{ matrix.build_cmd }}

      - uses: actions/upload-artifact@v7
        with:
          name: dist-${{ matrix.os }}
          path: |
            dist/*.exe
            dist/*.AppImage
            dist/*.snap
            dist/*.deb
            dist/*.dmg
            dist/*.zip
            dist/*.yml
            dist/*.yaml
          if-no-files-found: ignore
